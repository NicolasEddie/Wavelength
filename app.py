"""Streamlit app: target picker, stretch sliders, detection toggle.

TODO: load data/processed/*.npz (no network calls at runtime), let the
user pick a target, adjust per-channel stretch via compose.py, and
toggle a detect.py segmentation overlay.
"""
